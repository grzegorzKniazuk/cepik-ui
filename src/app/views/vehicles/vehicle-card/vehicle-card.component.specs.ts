import { VehicleCardComponent } from 'src/app/views/vehicles/vehicle-card/vehicle-card.component';

describe(VehicleCardComponent.name, () => {
    /*
    let component: VehicleCardComponent;
    let details: VehicleDetails;

    beforeEach(() => {
        details = {

        };

        TestBed.configureTestingModule(
            {
                providers: [
                    { provide: MODAL_DATA, useValue: details },
                    ChangeDetectorRef,
                ],
            },
        );

        component = TestBed.inject(MODAL_DATA);
    });
    */
});
